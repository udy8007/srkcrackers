/**
 * SEED DISABLED.
 * Catalog/admin data is managed in production Admin — never auto-seed.
 * This file is kept only so accidental `tsx prisma/seed.ts` fails loudly.
 */
console.error(
  "Seed is disabled. Product/category data must be managed in Admin — not via seed.",
);
process.exit(1);
