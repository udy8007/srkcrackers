/** Re-export Prisma domain types for existing `@/lib/db/types` imports. */
export type {
  AdminUser,
  Category,
  Product,
  Order,
  OrderItem,
  OrderStatusHistory,
  SiteVisit,
  EmailSettings,
  EmailLog,
  AdminNotification,
  AdminPushLog,
  BackupSettings,
  DbBackupLog,
  SchedulerState,
  AdminDeviceToken,
  FirebaseSettings,
  OrderStatus,
  UserRole,
  BackupFrequency,
} from "@prisma/client";
